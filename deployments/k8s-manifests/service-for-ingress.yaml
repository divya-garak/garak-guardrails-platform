apiVersion: v1
kind: Service
metadata:
  name: nemo-guardrails-service
  namespace: default
spec:
  selector:
    app: nemo-guardrails-secure  # Match existing deployment
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP  # For use with ingress